{
  "name": "back-end",
  "version": "1.0.0",
  "description": "PGA Players Review App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "db:seed": "psql -U postgres -f db/seed.sql",
    "db:init": "psql -U postgres -f db/schema.sql",
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "psql -U postgres -f db/schema.sql && psql -U postgres -f db/seed.sql"
  },
  "keywords": [],
  "author": "Derek",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "nodemon": "^2.0.20",
    "pg": "^8.8.0",
    "pg-promise": "^10.11.1"
  }
}
